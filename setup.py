from setuptools import setup, find_packages

setup(name='network-science-project', version='1.0', packages=find_packages())

# run pip install -e . in the project root dir to make lib.py available to other scripts in the project.